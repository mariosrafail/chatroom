{
  "name": "chatroom",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "check": "node --check app.js && node --check netlify/functions/messages.js"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.10.4"
  }
}
